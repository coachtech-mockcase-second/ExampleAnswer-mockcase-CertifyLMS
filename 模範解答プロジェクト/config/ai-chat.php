<?php

declare(strict_types=1);

return [
    /*
    |--------------------------------------------------------------------------
    | AI 相談機能の機能スイッチ
    |--------------------------------------------------------------------------
    | false に設定すると AI 相談ルート群が全て 404 になり、サイドバー項目と
    | フローティングウィジェットも自動非表示になる。本番でクォータ枯渇時の
    | 緊急停止スイッチとしても使用する。
    */
    'enabled' => env('AI_CHAT_ENABLED', true),

    /*
    |--------------------------------------------------------------------------
    | LLM ドライバ
    |--------------------------------------------------------------------------
    | 既定は gemini (Google Generative Language API)。将来別 LLM に切替する場合は
    | LlmRepositoryInterface の別実装を AppServiceProvider で binding する。
    */
    'driver' => env('AI_CHAT_DRIVER', 'gemini'),

    /*
    |--------------------------------------------------------------------------
    | Gemini API 接続情報
    |--------------------------------------------------------------------------
    */
    'gemini' => [
        'endpoint' => env('GEMINI_API_ENDPOINT', 'https://generativelanguage.googleapis.com/v1beta'),
        'api_key' => env('GEMINI_API_KEY'),
        'model' => env('GEMINI_MODEL', 'gemini-2.5-flash'),
    ],

    /*
    |--------------------------------------------------------------------------
    | Rate Limit / 履歴ウィンドウ
    |--------------------------------------------------------------------------
    */
    'daily_message_limit' => (int) env('AI_CHAT_DAILY_MESSAGE_LIMIT', 50),
    'history_window' => (int) env('AI_CHAT_HISTORY_WINDOW', 20),

    /*
    |--------------------------------------------------------------------------
    | 会話タイトル LLM 自動生成 (任意拡張)
    |--------------------------------------------------------------------------
    | false で完全無効化 (先頭 30 文字 fallback のみ)。Gemini クォータ逼迫環境で off にする。
    */
    'title_generation_enabled' => env('AI_CHAT_TITLE_GENERATION_ENABLED', true),

    'title_generation_prompt' => <<<'PROMPT'
あなたは会話タイトル生成 AI です。以下の AI チャット会話の最初のやり取りを読み、簡潔で内容を端的に表す日本語タイトルを生成してください。

制約:
- 30 文字以内(必須)
- 末尾の「？」「！」等の記号は使わない(句読点は OK)
- 「〜について」「〜の質問」等の冗長な表現は避ける
- 専門用語があれば優先的にタイトルに含める
- 余計な前置きや説明は付けず、タイトル本文のみを返す(前後の引用符・改行も不要)

例:
- ユーザー: 「Spring Security の認証フィルタの順序がよく分からない」
- タイトル: Spring Security 認証フィルタの順序
PROMPT,

    /*
    |--------------------------------------------------------------------------
    | システムプロンプトテンプレート
    |--------------------------------------------------------------------------
    | プレースホルダ {user_name} {certification_context} {section_context} は
    | AiChatPromptBuilderService::build() で動的置換される。資格 / 教材コンテキストは
    | 該当しない場合は空文字で置換される (= 出力から自然に消える)。
    */
    'system_prompt_template' => <<<'PROMPT'
あなたは Certify LMS の学習支援アシスタントです。
受講生「{user_name}」さんの資格試験合格をサポートしてください。

{certification_context}

{section_context}

応答方針:
- 必ず日本語で回答してください。
- 正答の暗記ではなく、理解を促すヒントや解説を心がけてください。
- 不適切・差別的・違法な要求には応じないでください。
- 受講生が読んでいる教材の文脈がある場合、その内容を踏まえて回答してください。
PROMPT,
];
