# {チケットID} {タイトル}

## メタ情報

| 項目 | 値 |
|---|---|
| チケット ID | `S-B-XX` / `S-A-XX` |
| Feature 連番 | `{Feature略称}-{連番}` |
| Feature | {Feature 名} |
| 種別 | Story |
| サブカテゴリ | 新規機能の構築 / 既存機能の拡張 |
| 難易度 | Basic / Advance |
| 工数 (h) | XX |
| 依存チケット | (なし / `S-B-XX`) |

## 背景・目的

(現状の課題と達成したい状態を 1〜2 段落の自然な文章で。なぜ今やるかも含む)

## ユーザーストーリー

> ペルソナは LMS プロダクト内ロール(受講生 / コーチ / 管理者)のみを使う。開発者 / API 学習者 / COACHTECH 受講生 等のメタペルソナは禁止(規約 7.1 参照)。API チケット等でエンドユーザー操作を伴わない場合でも LMS ロール視点で書く。

- **{ロール A}として**、{何を}したい。なぜなら、{目的}から。
- **{ロール B}として**、{何を}したい。なぜなら、{目的}から。

## 要件

> チケットの機能スコープを完全に表現(機能 + 認可 + 入力検証 + 副作用 + エッジケース挙動)。**HTTP ステータス / URL パス / 技術用語(Resource / Policy / Action 等)は書かない**(規約 4 章参照)。サブセクション分けは規模により任意、業務語彙ベースの大カテゴリで分ける。要件 ⊇ 受け入れ条件 の関係を保つ。

### {業務語彙の機能群}(該当ロール / 当事者制約)

- (機能 1: 業務的な動詞 + 機能と密接な制約)
- (機能 2: 副作用も明示)

### アクセス制御(機能群共通、該当時のみ)

- (横断的なロール / 状態制約)

### 非機能要件(該当時のみ、出てこなければ削除)

- (性能 / レート制限 / セキュリティ強度 / 運用観測 等)

## スコープ外

- (スコープ外 1)
- (スコープ外 2)

## 受け入れ条件

> 各項目 = 評価シート ① の 1 採点行と 1:1 対応(規約 3 章参照)。
> **件数目安**: 通常 Story **4-7 件** / 規模大 **6-10 件**。
> **順序**: 機能の主要な振る舞い → 重要なエッジケース → 副作用 / 状態遷移 → 入力検証 → 認可(規模・自然さで柔軟)。

- [ ] {正常系 happy path 1: 具体的な振る舞いで記述、「適切な案内」「正しく実装され」のような抽象表現は避ける}
- [ ] {正常系 happy path 2}
- [ ] {重要なエッジケース(該当時)}
- [ ] {副作用 / 状態遷移(該当時)}
- [ ] {入力検証(該当時、業務語彙ルール一覧をネスト):
  - {項目 1}: {必須 / 200 文字以内 / 形式 等の業務語彙ルール}
  - {項目 2}: {同上}}
- [ ] {認可(該当時、機能群ごとに 1 件で統合が原則)}
- [ ] 本チケットの機能に対するテスト (Unit / Feature 等) が実装されている

## 実装方針(参考)

> **本セクションは「参考」、受講生ごとに異なる実装を許容**(AC を満たせば実装手段は問わない)。ただし **「(必須)」マーカー付きサブセクション**(インターフェース / データモデル > 初期データ Seeder)は AC・採点・動作確認のベース、ここに記載した内容を正確に実装する。
>
> **サブセクション順序の論理**(外部→内部の潜行順): 受け入れ条件で「何が起きるべきか」を読んだ読者が、次に「どこから呼ばれて何が起きる(インターフェース)→ 何が永続化される(データモデル)→ どこに何の実装がある(コンポーネント)→ 異常時はどうなる(異常系)→ なぜこの作り(設計判断)」と外側から内側へ潜る流れに揃える。

### インターフェース(必須)

**エンドポイント**:

| HTTP | パス | 認可 | 振る舞い |
|---|---|---|---|
| GET | `/xxx` | {受講生 / コーチ / 管理者} | 一覧画面表示 |
| POST | `/xxx` | {受講生のみ 等} | 新規作成、リダイレクト + フラッシュ「{文言}」 |
| GET | `/xxx/{id}` | {当事者のみ 等} | 詳細画面表示 |
| PATCH | `/xxx/{id}` | {当事者のみ 等} | 更新、リダイレクト + フラッシュ「{文言}」 |
| DELETE | `/xxx/{id}` | {当事者のみ 等} | 削除、リダイレクト + フラッシュ「{文言}」 |

> 認可拒否は基本 403(リソース不在は 404)。認可列に「当事者(投稿者本人)」「ロール × スコープ(コーチ × 担当資格)」「不許可ロール(管理者は不可)」を 1 セルに圧縮して記述する。

**ミドルウェア**: `{認証 + ロール + 状態 等}` 全ルート適用 / `{個別ルートのレート制限 等}`

**機能 OFF 時** (該当時):
- ルート群を未登録にして 404 化 / 該当画面に「現在利用できません」を表示 等

### データモデル(新規テーブル時のみ)

**エンティティ**:

| エンティティ (Model) | 主要属性 | 関係性 | 制約 |
|---|---|---|---|
| {業務語彙} (`{Model1}`) | {属性 1 / 属性 2 / 属性 3}(業務語彙) | {親エンティティ}に所属 / {関連 Entity}に関連 | FK `restrictOnDelete` / SoftDelete 不採用 / 物理削除のみ |
| {業務語彙} (`{Model2}`) | {属性 1 / 属性 2} | {Model1}に所属 | FK `cascadeOnDelete` / SoftDelete 採用 |

> **制約列に集約する情報**: 外部キーの ON DELETE 戦略 / SoftDelete 採否 / 物理削除/論理削除の選択 / 連動削除の仕組み(Action 内 2 段階削除 等)。データ整合性に関する判断はここに 1 箇所だけ書く。

**Enum**:

- {業務語彙} (`{Enum1}`): {値 1 業務語彙} (`{Key1}`) / {値 2 業務語彙} (`{Key2}`) / {値 3 業務語彙} (`{Key3}`)
- {業務語彙} (`{Enum2}`): ...

**インデックス用途**:

- {軸 1 × 軸 2}({用途})

**初期データ Seeder(必須)**:

- {データパターン要約、件数 + 状態バリエーション}
- {動作確認用途の対応関係: AC のどの項目を実機確認できるか}
- DatabaseSeeder 順序: {依存 Seeder} の後

### コンポーネント

> レイヤー別に **クラス名 + ファイルパス + 1 行責務** を集約。Step 4 引き算 SSoT として、ここを読めば「何のファイルが存在し、どのレイヤーに何の責務があるか」が一目で把握できる粒度にする。**クラス列挙はこのセクションに 1 箇所のみ**(他セクションで再列挙しない)。

**Controller** (`app/Http/Controllers/`)
- `{Feature}Controller` — {責務、例「HTTP 受付 → 認可委譲 → Action 呼出 → レスポンス整形」、対応する method 群}

**FormRequest** (`app/Http/Requests/{Feature}/`)
- `IndexRequest` / `StoreRequest` / `UpdateRequest` — {対応操作}

**Action** (`app/UseCases/{Feature}/`、※ 模範解答 PJ で採用、Basic 受講生は Controller 内完結も可)
- `{Op1}Action` / `{Op2}Action` — {主要業務処理を 1 行で}

**Service** (`app/Services/`、新規導入時のみ)
- `{ServiceName}Service` — {責務}

**Repository** (`app/Repositories/`、新規導入時のみ、外部 API 切り離し)
- `{Contract}Interface` + `{Impl}Repository` — {責務}

**Policy** (`app/Policies/`)
- `{Model}Policy` — {認可判定軸の要約、例「ロール × 当事者 × 担当資格の三重判定」}

**Notification** (`app/Notifications/`、該当時のみ)
- `{Notification}` — {発火条件 / 配信チャネル}

**Model + Enum** (`app/Models/`, `app/Enums/`)
- `{Model1}` / `{Model2}` / `{Enum1}` / `{Enum2}`

**View**(提供 PJ 既存、ロック対象)
- `resources/views/{feature}/*.blade.php` + partials

**Migration / Seeder**
- `database/migrations/*_create_{table}_table.php`
- `database/seeders/{Seeder}.php`

**例外** (`app/Exceptions/{Feature}/`)
- `{Exception1}Exception` / `{Exception2}Exception`

**Routes** (`routes/web.php` or `routes/api.php`)
- {ルート名 prefix / グループ構成、例「公開 `qa-board.*` + 管理者モデレーション `admin.qa-board.*`」}

### 異常系

**入力検証**(FormRequest クラス名 + ルール記法):

- **{操作名}** (`{Feature}\{Op}Request`):
  - `{field_1}`: `required` / `string` / `max:200`
  - `{field_2}`: `nullable` / `integer` / `min:1` / `max:100`

> 推奨エラーメッセージの日本語文言例は **想定 Q&A セクションに集約**(本セクションは機械的ルール記法のみ)。

**業務例外**(状態ベースガード + HTTP ステータス):

- {業務語彙の制約} (`{ExceptionClass}`) → 409
- {制約 2} (`{ExceptionClass2}`) → 422

**外部 API フォールバック**(該当時のみ):

- {例: AI 応答失敗時、受講生入力は保持、AI 応答はエラー状態で保存}

### 設計判断(該当時のみ)

> 特殊設計判断 + **採用根拠**(なぜを書く、何をは他セクションで)。**Basic チケット共通の前提(JS なし / XSS 防御 / Pint 整形 等)は書かない**(`.claude/rules/` 既存ガイドに委譲)。本チケット固有のトレードオフがある責務分割・共通化・採用技術のみ記述。
>
> **テスト観点**: 標準的な Feature テストパターン以外で本チケット固有の特殊観点(認可マトリクス網羅 / 並行性検証 / 計算境界 等)があれば 1〜2 行で触れる。標準パターンのみなら省略。

- **{判断 1、例: 公開と管理者モデレーションで Controller 共通化}**: {採用根拠、例「ロール別ファイル分割は将来のロール追加時のリネームコストが大きく、Policy + Route 名で出し分けるパターンが将来コスト最小」}
- **{判断 2、例: 状態整合性の同時更新}**: {採用根拠、例「`status` Enum と日時カラムを別 UPDATE にすると不整合の温床になる」}
- **テスト観点(該当時)**: {例「Policy 三重判定のケース網羅 + 並行リクエスト時の TOCTOU 検証は標準パターン外」}

## 補足

### 想定ヒアリング Q&A

| 質問 | 回答 |
|---|---|
| (バリデーション仕様: 最大文字数 / 必須 / 任意 / 上下限値) | ... |
| (認可振る舞い: 当事者判定 / HTTP ステータス) | ... |
| (振る舞い詳細: フラッシュ表示 / リダイレクト先 / 副作用) | ... |
| (文言の推奨例: フラッシュ・エラーメッセージ — 本セクションが文言推奨例の SSoT) | ... |
| (状態 × ロールで振る舞いが分岐する条件) | ... |
