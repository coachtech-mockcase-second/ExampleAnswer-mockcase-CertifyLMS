# {チケットID} {タイトル}

## メタ情報

| 項目 | 値 |
|---|---|
| チケット ID | `S-B-XX` / `S-A-XX` |
| Feature 連番 | `{Feature略称}-{連番}` |
| Feature | {Feature 名} |
| 種別 | Story |
| サブカテゴリ | 新規機能の構築 / 既存機能の拡張 |
| 難易度 | Basic / Advance |
| 工数 (h) | XX |
| 依存チケット | (なし / `S-B-XX`) |

## 背景・目的

(現状の課題と達成したい状態を 1〜2 段落の自然な文章で。なぜ今やるかも含む)

## ユーザーストーリー

- **{ロール A}として**、{何を}したい。なぜなら、{目的}から。
- **{ロール B}として**、{何を}したい。なぜなら、{目的}から。

## やること

> 機能・操作の箇条書き。サブセクション分けは任意(規模が大きい場合に業務語彙ベースで大カテゴリ分け)。**認可挙動・HTTP ステータス・状態遷移詳細は記載しない**(実装方針 > 認可方針 / 受け入れ条件 で網羅する)。

### {業務語彙の機能カテゴリ}

- (機能 1: 業務的な動詞ベース。例「面談パックの新規作成・編集・削除」)
- (機能 2)

### {別の業務語彙の機能カテゴリ}

- (機能 3)
- (機能 4)

## やらないこと

- (スコープ外 1)
- (スコープ外 2)

## 受け入れ条件

- [ ] {正常系 happy path 1: 「{URL} にアクセスした際に、{何が} {どうなる} か」「{操作} を行った際に、{副作用} + {表示} がされる」など、具体的な振る舞いで記述}
- [ ] {正常系 happy path 2}
- [ ] {重要なエッジケース(該当時): 「{機能} が OFF の場合、{具体的な振る舞い}(画面非表示 / エラー表示 / 拒否 等)」のように具体化。「適切な案内」「アクセス拒否」のような抽象表現は避ける}
- [ ] {副作用 / 状態遷移(該当時): 「{操作} を行った際に、{配下リソース} も連動削除される」など}
- [ ] {入力検証(該当時): 「{フィールド}(空 / 長すぎる / 形式不正 等)を入力した際に、バリデーションエラーが表示され、保存・送信されない」(振る舞いベース、機械値 `max:200` は書かない)}
- [ ] {認可(オーナー / 当事者): 「{誰}のみが{何}でき、他人が直アクセスすると 403 が返る」(振る舞いベース、技術名 `Policy` 等は書かない)}
- [ ] {認可(ロール / アクセス制限): 「{誰}(コーチ / 管理者 / 退会済 等)が {URL} にアクセスすると 403 が返る」}

## 実装方針(参考)

### 主要 URL

| 業務操作 | URL |
|---|---|
| (一覧画面) | `/xxx` |
| (詳細画面) | `/xxx/{id}` |
| (新規作成 / 編集 / 削除 / 状態遷移) | `/xxx/...` |

### 認可方針

- 管理者: ...
- コーチ: ... (担当資格スコープ 等)
- 受講生: ... (自分のみ / 受講登録あり 等)
- 状態ベース制約: (例「公開中の{Entity}は削除不可」)

### データモデル(新規テーブル時のみ)

- {エンティティ名}: 主要属性(業務語彙)+ リレーション概念
- 例:「面談パック: 名前 / 面談回数 / 状態(下書き / 公開中 / アーカイブ) / 作成者 / 更新者」

### Seeder 設計(新規テーブル時のみ)

- 状態別件数バリエーション(例: 公開中 × 3 + 下書き × 1 + アーカイブ × 1)
- 採点動作確認に必要なデータパターンを業務語彙で記述

### API 仕様(該当時のみ)

- {エンドポイント概念}: 認証要否 / リクエスト概要 / レスポンス概要

### 関連ファイル

- (コードリーディング起点となる主要ファイルパス)

## 補足

### 想定ヒアリング Q&A

| 質問 | 回答 |
|---|---|
| (バリデーション仕様: 最大文字数 / 必須 / 任意 / 上下限値) | ... |
| (認可振る舞い: 当事者判定 / HTTP ステータス) | ... |
| (振る舞い詳細: フラッシュ表示 / リダイレクト先 / 副作用) | ... |
| (文言の推奨例: フラッシュ・エラーメッセージ) | ... |
| (状態 × ロールで振る舞いが分岐する条件) | ... |
