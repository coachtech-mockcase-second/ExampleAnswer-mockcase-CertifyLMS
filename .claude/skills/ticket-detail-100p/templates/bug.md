# {チケットID} {タイトル}

<!--
記述粒度規約: 実装粒度(テーブル名 / カラム名 / クラス名 / SQL 詳細 / Laravel 実装語彙 / URL パス詳細 / HTTP メソッド名 / Middleware・例外クラス名 等)を記載できるのは `## 実装方針` 配下のみ。それ以外のセクション(概要 / 再現手順 / 期待する動作 / 実際の動作 / 補足)は **業務語彙のみ** で記述する。詳細規約は `../../../.claude/rules/ticket-spec.md`「実装粒度の記載範囲」参照。
再現手順 / 期待する動作 / 実際の動作 では「○○画面を開く」「200 が返って一覧が表示される」「403 が返る」のように業務語彙 + HTTP ステータス(振る舞いの一部)で記述する。URL パスやテーブル名は実装方針内へ。
受け入れ条件は振る舞いベース(リダイレクト先 URL のパス / HTTP ステータス / 状態名は振る舞いの一部として許容)。
-->

## メタ情報

| 項目 | 値 |
|---|---|
| チケット ID | `B-B-XX` / `B-A-XX` |
| Feature 連番 | `{Feature略称}-{連番}` |
| Feature | {Feature 名} |
| 種別 | Bug |
| サブカテゴリ | 認可・認証 / データ / 機能 / UI/UX / セキュリティ / 並行性 |
| 難易度 | Basic / Advance |
| 工数 (h) | XX |
| 依存チケット | (なし / `S-B-XX`) ← **このチケットが依存する側** を記録(逆向きは書かない) |

## 概要

> 受講生が観測する不具合の概要を 1〜2 文で。原因・修正方針は書かない。

(1〜2 文)

## 再現手順

> バグを再現する操作ステップ。前提状態は冒頭 1 行で明示し、続けて 1 アクション = 1 ステップで番号付き。アプリ環境は dev (Sail) 固定なので明記不要。

**前提**: (該当ロールでログイン済み、再現に必要な Seeder データ・状態を 1 行で記述。例「受講生 A でログイン、資格 X に登録済」)

1. (操作)
2. (操作)
3. (バグが観測される時点の操作)

## 期待する動作

> 本来あるべき正しい振る舞い(Expected)。

(1〜2 文)

## 実際の動作

> 現状観測される誤った振る舞い(Actual)。「期待する動作」と直接対比できる粒度で。

(1〜2 文)

## 受け入れ条件

- [ ] {Bug 固有の振る舞い 1: 例「受講生 A が他人 B の個人目標編集 URL に直叩きすると 403 が返る」}
- [ ] {Bug 固有の振る舞い 2: 任意}

## 実装方針

> **参考設計の一例**。受け入れ条件を満たせれば実装手段は問わない。受講生は提供 PJ コード + ヒアリングで自分の調査・修正方針を組み立てる。

### 主要 URL

> バグが発生する画面の HTTP メソッド × パス × 振る舞いを記載。

| メソッド | パス | 振る舞い |
|---|---|---|
| GET | `/xxx` | バグが発生する画面の操作 |

### 原因箇所メモ

- 原因の主要ファイル: `app/Http/Controllers/{Controller}.php`
- (該当する Policy / Middleware / FormRequest / Action があれば追記)
- 仕込み内容(Step 4 引き算): (具体的な仕込み方を 1〜2 文で。例「Controller の `where('status', 'published')` 条件を削除」)
- 受講生が辿るべき修正範囲: (どの粒度の修正を期待するか 1 文)

## 補足

### 想定ヒアリング Q&A(任意)

| 質問 | 回答 |
|---|---|
| ... | ... |
