# {チケットID} {タイトル}

## メタ情報

| 項目 | 値 |
|---|---|
| チケット ID | `B-B-XX` / `B-A-XX` |
| Feature 連番 | `{Feature略称}-{連番}` |
| Feature | {Feature 名} |
| 種別 | Bug |
| サブカテゴリ | 認可・認証 / データ / 機能 / UI/UX / セキュリティ / 並行性 |
| 難易度 | Basic / Advance |
| 工数 (h) | XX |
| 依存チケット | (なし / `S-B-XX`) ← **このチケットが依存する側** を記録(逆向きは書かない) |

## 概要

> 受講生が観測する不具合の概要を 1〜2 文で。原因・修正方針は書かない。

(1〜2 文)

## 再現手順

> バグを再現する操作ステップ。前提状態は冒頭 1 行で明示し、続けて 1 アクション = 1 ステップで番号付き。アプリ環境は dev (Sail) 固定なので明記不要。

**前提**: (該当ロールでログイン済み、再現に必要な Seeder データ・状態を 1 行で記述。例「受講生 A でログイン、資格 X に登録済」)

1. (操作)
2. (操作)
3. (バグが観測される時点の操作)

## 期待する動作

> 本来あるべき正しい振る舞い(Expected)。

(1〜2 文)

## 実際の動作

> 現状観測される誤った振る舞い(Actual)。「期待する動作」と直接対比できる粒度で。

(1〜2 文)

## 主要 URL

> バグが発生する画面の HTTP メソッド × パス × 振る舞いを記載。

| メソッド | パス | 振る舞い |
|---|---|---|
| GET | `/xxx` | バグが発生する画面の操作 |

## 受け入れ条件

- [ ] {Bug 固有の振る舞い 1: 例「受講生 A が他人 B の個人目標編集 URL に直叩きすると 403 が返る」}
- [ ] {Bug 固有の振る舞い 2: 任意}

## 補足

> Bug は修正方針を渡さないため「実装方針」セクションは持たない。

### 原因箇所メモ(コーチ用、受講生に直接教えない)

> **コーチが Bug の原因コードを事前把握しておく材料**。受講生のヒアリング応答時は **ファイルパスや修正コードを直接教えない**(教育上 NG)。「○○の挙動が想定と異なるが、どのファイル・どのレイヤーを調査した?」「○○の状態でどんな処理が走るか追えた?」のような **問い返し / 方向性ヒント** で受講生自身のコードリーディングを促すための材料として使う。
> Bug 修正範囲の Basic / Advance 別ルールは `../README.md#bug-修正範囲構築側の仕込み制約` 参照。

- 原因の主要ファイル: `app/Http/Controllers/{Controller}.php`
- (該当する Policy / Middleware / FormRequest / Action があれば追記)

### 想定ヒアリング Q&A

> 3 階層(必須回答 / 実装判断 / 補足)で記述(Bug は規模が小さい場合、階層を簡略化可)。受講生の **仮説提案型ヒアリング**(「○○と設計しましたが問題ないですか?」)に対して OK / NG を返すスタイル。

#### 必須回答(原因領域・修正範囲・想定挙動)

| 質問 | 回答 |
|---|---|
| ... | ... |

#### 実装判断(設計レベル)

| 質問 | 回答 |
|---|---|
| ... | ... |

#### 補足

| 質問 | 回答 |
|---|---|
| ... | ... |
