# {チケットID} {タイトル}

## メタ情報

| 項目 | 値 |
|---|---|
| チケット ID | `B-B-XX` / `B-A-XX` |
| Feature 連番 | `{Feature略称}-{連番}` |
| Feature | {Feature 名} |
| 種別 | Bug |
| サブカテゴリ | 認可・認証 / データ / 機能 / UI/UX / セキュリティ / 並行性 |
| Severity | Critical / Major / Minor (技術影響度、業務緊急度ではない) |
| 難易度 | Basic / Advance |
| 工数 (h) | XX |
| 依存チケット | (なし / `S-B-XX`) ← **このチケットが依存する側** を記録(逆向きは書かない) |

## 概要

> 受講生が観測する不具合の概要を 1〜2 文で。原因・修正方針は書かない。

(1〜2 文)

## 再現手順

> バグを再現する操作ステップ。1 アクション = 1 ステップで番号付き。

1. (操作)
2. (操作)
3. (バグが観測される時点の操作)

## 期待する動作

> 本来あるべき正しい振る舞い(Expected)。

(1〜2 文)

## 実際の動作

> 現状観測される誤った振る舞い(Actual)。「期待する動作」と直接対比できる粒度で。

(1〜2 文)

## 環境

| 項目 | 値 |
|---|---|
| アプリ環境 | dev (Sail) / staging / prod |
| ロール | student / coach / admin (該当ロールを明示) |
| その他の前提 | 特別な前提条件があれば(例: 特定の Seeder データ、特定の状態) |

## 受け入れ条件

> Bug 固有 AC + 共通 DoD の二段構造。書き方原則は `../README.md#受け入れ条件の書き方原則` 参照(振る舞いベース + 1 項目 1 振る舞い)。

**Bug 固有 AC**(1 項目以上、Bug ごとの固有振る舞い):

- [ ] {Bug 固有の振る舞い 1: 例「受講生 A が他人 B の個人目標編集 URL に直叩きすると 403」}
- [ ] {Bug 固有の振る舞い 2: 任意}

**共通 DoD**:

- [ ] 再現手順で「期待する動作」が観測される(症状が解消されている)
- [ ] 既存の他機能が壊れていない(回帰なし)
- [ ] バグ再現テスト + 修正後テストが pass

<!-- coach-only:start -->

## 補足

> Bug は修正方針を渡さないため「実装方針(参考)」セクションは持たない。主要 URL は本セクション配下に集約。

### 主要 URL

| メソッド | パス | 説明 |
|---|---|---|
| GET | `/xxx` | バグが発生する画面 |

### 主要関連ファイル (コーチ即答用)

> 修正範囲のコード起点。受講生から「どこを直す?」と聞かれた時の案内用。
> Bug 修正範囲の Basic / Advance 別ルールは `../README.md#bug-修正範囲構築側の仕込み制約` 参照。

- `app/Http/Controllers/{Controller}.php`
- `app/UseCases/{Feature}/{Action}.php`
- (該当する Policy / Middleware / FormRequest があれば追記)

### 想定ヒアリング Q&A

> 3 階層(必須回答 / 実装判断 / 補足)で記述(Bug は規模が小さい場合、階層を簡略化可)。コーチが受講生からヒアリングされた時の即答材料。

#### 必須回答(原因領域・修正範囲・想定挙動)

| 質問 | 回答 |
|---|---|
| ... | ... |

#### 実装判断(設計レベル)

| 質問 | 回答 |
|---|---|
| ... | ... |

#### 補足

| 質問 | 回答 |
|---|---|
| ... | ... |

<!-- coach-only:end -->
