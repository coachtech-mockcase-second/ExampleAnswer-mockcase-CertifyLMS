# {チケットID} {タイトル}

## メタ情報

| 項目 | 値 |
|---|---|
| チケット ID | `B-B-XX` / `B-A-XX` |
| Feature 連番 | `{Feature略称}-{連番}` |
| Feature | {Feature 名} |
| 種別 | Bug |
| サブカテゴリ | 認可・認証 / データ / 機能 / UI/UX / セキュリティ / 並行性 |
| 難易度 | Basic / Advance |
| 工数 (h) | XX |
| 依存チケット | (なし / `S-B-XX`) |

## 概要

(1〜2 文で、観測される不具合の内容と影響)

## 再現手順

**前提**: (該当ロールでログイン済み、再現に必要な Seeder データ・状態を 1 行で)

1. (操作)
2. (操作)
3. (バグが観測される時点の操作)

## 期待する結果

(1〜2 文、本来あるべき正しい振る舞い)

## 実際の結果

(1〜2 文、現状観測される誤った振る舞い)

## 受け入れ条件

- [ ] {Bug 固有の振る舞い 1(期待する結果が達成される観察可能事象)}
- [ ] {Bug 固有の振る舞い 2、任意}
- [ ] {Bug 固有の振る舞い 3、任意}
- [ ] 本チケットの機能に対するテスト (Unit / Feature 等) が実装されている

## 実装方針(参考)

> **粒度**: 業務語彙で原因要約 + 主要ファイルパス・クラス名・メソッド名を併記。修正コード片 / SQL 詳細は書かない(コードリーディングは受講生の作業)。Step 4 仕込み方の詳細は **README の「Step 4 仕込み方」列が SSoT**(本セクションは採点者・コーチ向けの業務理解用)。

### 原因

- **主要ファイル**: `app/Http/Controllers/{Controller}.php` の `{methodName}()` 等(該当する Policy / Middleware / FormRequest / Action / Service があれば追記、業務語彙の責務も併記)
- **仕込み内容**: ({業務語彙で 1〜2 文、例: 「公開判定 (`PartPolicy::view`) が下書き状態を許容している」「並行更新時の整合性チェック (`MeetingQuota\\ConsumeQuotaAction` の `lockForUpdate`) 漏れ」})
- **修正範囲**: ({どの粒度の修正を期待するか 1 文、例: 「該当 Controller method の数行修正で完結」「Action 内 1 行追加で完結」})

## 補足

### 想定 Q&A(任意)

| 質問 | 回答 |
|---|---|
| (正しい振る舞い確認: ステータス / 当事者判定 等) | ... |
| (文言の推奨例: フラッシュ・エラーメッセージ) | ... |
