# {チケットID} {タイトル}

## メタ情報

| 項目 | 値 |
|---|---|
| チケット ID | `T-B-XX` / `T-A-XX` |
| Feature 連番 | `{Feature略称}-{連番}` |
| Feature | {Feature 名 / 横断} |
| 種別 | Task |
| サブカテゴリ | パフォーマンス / リファクタリング |
| 難易度 | Basic / Advance |
| 工数 (h) | XX |
| 依存チケット | (なし / `S-B-XX`) |

## 概要

(1〜2 段落で、現状の課題と達成したい状態を自然な文章で)

## 要件

- (作業範囲 1: 業務語彙 + 対象範囲)
- (作業範囲 2)

## スコープ外

- (スコープ外 1)
- (スコープ外 2)

## 受け入れ条件

> **件数目安**: 1-3 件(目的 + 該当時の検証項目 + テスト実装)。
> 振る舞い不変 / Before-After 計測値 / 既存テスト全 pass は AC に書かず評価シート ② 横断品質で扱う(規約 3.5)。

- [ ] {Task の目的が達成された観察可能事象 — Performance: N+1 が発生しない 等、Refactoring: 構造分離 / 削除の事実}
- [ ] {Task 固有の検証項目(該当時のみ、複数対象画面 / 認可 / 入力検証 等)}
- [ ] 本チケットの機能に対するテスト (Unit / Feature 等) が実装されている

## 実装方針(参考)

> **粒度**: 業務語彙 + 技術名(変更対象ファイルパス・クラス名・メソッド名)を併記。具体的なコード片 / SQL クエリ / メソッド完全例は書かない。

### 変更内容

- **変更対象ファイル**: `app/Http/Controllers/{Controller}.php` の `{methodName}()` / `app/UseCases/{Feature}/{Op}Action.php` の `__invoke()` 等(レイヤー別グルーピングで複数列挙可)
- **変更前**: ({現状を業務語彙で記述、例「一覧画面で各レコードのリレーション取得が N+1 (`{Feature}\\IndexAction` に `with()` 指定なし)」})
- **変更後**: ({理想形を業務語彙で記述、例「Eager Loading (`->with(['plan', 'enrollments'])`) で 2 クエリに集約」})
- **採用技術と判断理由**: ({技術概念とその採用根拠 + 代替案との比較、例「Eloquent `with()` で先読み」「Eloquent の標準パターンで保守性が高い、`load()` 後付けより一貫性あり」})
- **テスト観点(該当時のみ)**: ({標準的な振る舞い不変テスト以外で本 Task 固有の特殊観点があれば 1 行、例「Before/After のクエリ本数を `DB::listen()` で計測」})

## 補足

### 想定 Q&A(任意)

| 質問 | 回答 |
|---|---|
| (対象範囲・計測値・完了基準などで仕様確認が必要な事項) | ... |
