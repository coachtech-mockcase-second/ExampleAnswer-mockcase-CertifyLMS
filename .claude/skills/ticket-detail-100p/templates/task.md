# {チケットID} {タイトル}

## メタ情報

| 項目 | 値 |
|---|---|
| チケット ID | `T-B-XX` / `T-A-XX` |
| Feature 連番 | `{Feature略称}-{連番}` |
| Feature | {Feature 名 / 横断} |
| 種別 | Task |
| サブカテゴリ | パフォーマンス / リファクタリング |
| 難易度 | Basic / Advance |
| 工数 (h) | XX |
| 依存チケット | (なし / `S-B-XX`) ← **このチケットが依存する側** を記録(逆向きは書かない) |

## 概要

> 何を改善するか(What) を 1〜2 文で簡潔に。

## 背景・目的

> なぜこの改善が必要か(現状のどんな問題があるか) + 達成したい状態を 2〜4 文で。

## 対象範囲

- **対象画面 / 機能**: ...
- **対象レイヤー**: Controller / Service / Repository 等

## 受け入れ条件

> 各項目 = 1 採点行。Task は 3〜5 項目を目安。書き方原則は `../README.md#受け入れ条件の書き方原則` 参照(1 項目 1 振る舞い)。Performance は Before/After 計測値、Refactoring は構造記述で代替可。

- [ ] {計測値の改善 or 構造変更後の状態}
- [ ] 既存テストが pass する(振る舞い不変)
- [ ] 新規テストが追加されている

<!-- coach-only:start -->

## 実装方針 (参考)

> **あくまで参考設計**。様々な実装方法がある前提で、受講生は提供 PJ コード + ヒアリングで自分の設計を組み立てる。

### 主要 URL

| メソッド | パス | 説明 |
|---|---|---|
| GET | `/xxx` | ... |

### 対象詳細

- **変更対象ファイル候補**: `app/Http/Controllers/{Controller}.php` 等
- **変更前のコード状態** (Refactoring の場合、巻き戻し対象): ...
- **変更後の理想形** (Refactoring の場合): ...

### Before/After 計測方針 (Performance 専用)

- **計測手法**: Laravel Debugbar / `DB::enableQueryLog()` / Telescope / `microtime(true)` 等
- **計測ポイント**: クエリ数 / レスポンスタイム / メモリ使用量
- **目標値**: 例「クエリ数を 50 → 5 に削減」「レスポンスタイム 500ms → 100ms」

### テスト観点

> **チケット固有の検証観点**(横断品質「全テスト pass」「カバレッジ達成率」は評価シート ② 横断で別管理)。

| 種別 | 観点 |
|---|---|
| 振る舞い不変 | 既存 Feature テストが pass(リファクタ前後で同じ受け入れ条件を満たす) |
| 性能 (Performance) | Before/After 計測値の改善を assert(例: クエリ数 ≤ 5、レスポンスタイム ≤ 100ms) |
| 構造 (Refactoring) | 分割後の新クラス(Action / Service)の Unit テスト追加 / メソッド責務の単体確認 |

### アーキテクチャ判断

- **採用技術**: `with()` / `withCount()` / `chunk()` / `Cache::remember()` / Action 分離 / Service 分離 等
- **設計判断**:
  - ...

### 主要関連ファイル

> コードリーディング起点。受講生から「どこを見れば良い?」と聞かれた時の案内用。

- `app/Http/Controllers/{Controller}.php`
- `app/Services/{Service}.php`
- (Refactoring の場合)変更前のコード位置: ... → 移動先: ...

## 補足

### 想定ヒアリング Q&A

> 3 階層(必須回答 / 実装判断 / 補足)で記述。コーチが受講生からヒアリングされた時の即答材料。

#### 必須回答(対象範囲・計測値・完了基準)

| 質問 | 回答 |
|---|---|
| ... | ... |

#### 実装判断(設計レベル)

| 質問 | 回答 |
|---|---|
| ... | ... |

#### 補足

| 質問 | 回答 |
|---|---|
| ... | ... |

<!-- coach-only:end -->
