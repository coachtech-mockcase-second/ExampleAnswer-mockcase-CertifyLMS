# {チケットID} {タイトル}

<!--
記述粒度規約: 実装粒度(テーブル名 / カラム名 / クラス名 / SQL 詳細 / Laravel 実装語彙 / URL パス詳細 等)を記載できるのは `## 実装方針` 配下のみ。それ以外のセクション(概要 / 背景・目的 / やること / やらないこと / 補足)は **業務語彙のみ** で記述する。詳細規約は `../../../.claude/rules/ticket-spec.md`「実装粒度の記載範囲」参照。
受け入れ条件は構造記述 / Before/After 計測値ベース(Performance では計測値が振る舞いの代替指標、Refactoring では振る舞い不変 + 構造変更点)。
-->

## メタ情報

| 項目 | 値 |
|---|---|
| チケット ID | `T-B-XX` / `T-A-XX` |
| Feature 連番 | `{Feature略称}-{連番}` |
| Feature | {Feature 名 / 横断} |
| 種別 | Task |
| サブカテゴリ | パフォーマンス / リファクタリング |
| 難易度 | Basic / Advance |
| 工数 (h) | XX |
| 依存チケット | (なし / `S-B-XX`) ← **このチケットが依存する側** を記録(逆向きは書かない) |

## 概要

> 何を改善するか(What) を 1〜2 文で簡潔に。

## 背景・目的

> なぜこの改善が必要か(現状の技術的負債・性能課題・コード可読性等の問題) + 達成したい状態を 2〜4 文で。

## やること

> 改善内容を箇条書きで列挙(対象画面・対象操作・改善指標を含む)。3〜5 項目目安。

- (改善 1: 例「Admin ユーザー管理画面の N+1 解消」)
- (改善 2)

## やらないこと

> スコープ外を明示。後続チケットで対応するものは別チケット ID を併記。

- (スコープ外 1)

## 受け入れ条件

- [ ] {計測値の改善 or 構造変更後の状態}
- [ ] {Task 固有の検証項目}

## 実装方針

> **参考設計の一例**。受け入れ条件を満たせれば実装手段は問わない。受講生は提供 PJ コード + ヒアリングで自分の設計を組み立てる。

### 主要 URL

> 対象画面の HTTP メソッド × パス × 振る舞いを記載。Performance Task では必須(計測対象画面を特定するため)。Refactoring Task で対象画面が特定できない場合(例: 横断的なクラス分離・テスト追加)は **本サブセクションごと省略可**。

| メソッド | パス | 振る舞い |
|---|---|---|
| GET | `/xxx` | ... |

### 変更対象と変更前後の状態

- **変更対象ファイル候補**: `app/Http/Controllers/{Controller}.php` 等
- **変更前の状態**(Refactoring の場合、現状の問題コード状態): ...
- **変更後の理想形**(Refactoring の場合): ...

### 計測指標と目標値(Performance 専用)

> Performance Task では、**改善前(Before)の現状性能** と **改善後(After)の目標性能** を具体的な数値で示し、これが採点根拠となる(PR に Before/After 計測値の記載を受講生に求める)。

| 項目 | 内容 |
|---|---|
| 計測手法 | Laravel Debugbar / `DB::enableQueryLog()` / Telescope / `microtime(true)` 等 |
| 計測指標 | クエリ数 / レスポンスタイム / メモリ使用量 等 |
| 改善前(Before) | 例「クエリ数 50 / レスポンスタイム 500ms」 |
| 改善後(After 目標) | 例「クエリ数 5 / レスポンスタイム 100ms」 |

### テスト方針

> チケット固有の検証観点(横断品質は評価シート ② 横断で別管理)。

| 種別 | 観点 |
|---|---|
| 振る舞い不変 | 既存 Feature テストが pass(リファクタ前後で同じ受け入れ条件を満たす) |
| 性能(Performance) | Before/After 計測値の改善を assert(例: クエリ数 ≤ 5、レスポンスタイム ≤ 100ms) |
| 構造(Refactoring) | 分割後の新クラスの Unit テスト追加 / メソッド責務の単体確認 |

### 採用技術と判断理由

- **採用技術**: `with()` / `withCount()` / `chunk()` / `Cache::remember()` / Action 分離 / Service 分離 等
- **判断理由**:
  - ...

### 改善対象コードメモ(コーチ用、受講生に直接教えない)

> **コーチが改善対象コードを事前把握しておく材料**。受講生のヒアリング応答時は **ファイルパスや改善方法を直接教えない**(教育上 NG、受講生は自分で問題箇所を発見し設計判断するのが学習目的)。「現状コードのどこに N+1 がある?」「リファクタリングの責務分割をどう考えた?」のような **問い返し / 方向性ヒント** で受講生のコードリーディングを促すための材料として使う。

- 改善対象の主要ファイル: `app/Http/Controllers/{Controller}.php`
- (Refactoring の場合)変更前のコード位置: ... → 移動先: ...

## 補足

### 想定ヒアリング Q&A(任意)

> 受講生の **仮説提案型ヒアリング**(「○○と振る舞うべきと理解しましたが正しいですか?」)に対して OK / NG / 別案 を返すスタイル。**要件・仕様レベル(正しい振る舞いの確認)のみ** 記述する(設計判断 / 実装詳細は書かない、粒度規約は `../../../.claude/rules/ticket-spec.md`「想定ヒアリング Q&A の粒度」参照)。
> Task は Refactoring が振る舞い不変 / Performance が計測値中心で仕様確認の余地が小さいため、追加情報がなければ **本セクションごと省略可**。

| 質問 | 回答 |
|---|---|
| (対象範囲・計測値・完了基準などで仕様確認が必要な事項) | ... |
