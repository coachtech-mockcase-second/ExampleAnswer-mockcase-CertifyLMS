# B-B-02 教材管理の階層一覧（Part / Chapter / Section）が並び順を無視して表示される

## メタ情報

| 項目 | 値 |
|---|---|
| チケット ID | `B-B-02` |
| Feature 連番 | `content-management-02` |
| Feature | content-management |
| 種別 | Bug |
| サブカテゴリ | 機能(ソート) |
| 難易度 | Basic |
| 工数 (h) | 3.5 |
| 依存チケット | (なし) |

## 概要

管理者(admin) / コーチ(coach)が教材管理の各階層一覧(Part 一覧 / Part 詳細画面の Chapter 一覧 / Chapter 詳細画面の Section 一覧)を開くと、教材管理で設定した並び順ではなく、登録した順番で並んで表示される。コーチが意図した学習順序が画面に反映されず、教材の並びが崩れて見える。

## 再現手順

**前提**: 管理者またはコーチでログイン済み。ある資格に Part / Chapter / Section が複数登録されており、各階層で並び順の設定が登録順と一致していない(例: 後から登録した項目の並び順を先頭に設定済み)。

1. 当該資格の教材管理(Part 一覧)画面を開く → Part が並び順設定を無視して登録順(古い順)で並ぶ
2. ある Part の詳細画面を開く → 配下の Chapter が並び順設定を無視して登録順で並ぶ
3. ある Chapter の詳細画面を開く → 配下の Section が並び順設定を無視して登録順で並ぶ
4. いずれの階層も、並び順を変更しても一覧の表示順は登録順のまま変わらない

## 期待する結果

各階層の一覧(Part / Chapter / Section)は、教材管理で設定した並び順(順序番号の昇順)で表示される。並び順を変更すると、一覧の表示順もそれに追従する。

## 実際の結果

すべての階層で並び順設定が無視され、Part / Chapter / Section が登録した順番で表示される。コーチが並び替えで意図した学習順序が一覧に反映されない。

## 受け入れ条件

- [ ] 教材管理の以下の一覧が、いずれも設定された並び順(順序番号の昇順)で表示され、並び順を変更した後に再表示すると表示順が変更後の並びに追従する:
  - Part 一覧画面の Part
  - Part 詳細画面の Chapter 一覧
  - Chapter 詳細画面の Section 一覧

## 実装方針(参考)

### 原因

- **主要ファイル**: 各階層の一覧取得を担う 3 つの Action にそれぞれ整列指定が抜けている。
  - `app/UseCases/Part/IndexAction.php`(Part 一覧)
  - `app/UseCases/Part/ShowAction.php`(Part 詳細画面の Chapter 一覧)
  - `app/UseCases/Chapter/ShowAction.php`(Chapter 詳細画面の Section 一覧)
- **仕込み内容**: 各一覧取得クエリから並び順の整列指定(対象 Model の `scopeOrdered()` = 並び順カラム `order` の昇順)が抜けている。整列指定がないため Eloquent は既定(主キー / 登録順)で返し、コーチが設定した並び順が無視される。整列を書く場所は階層で 2 種類ある: Part 一覧は取得クエリのトップレベル(`->ordered()`)、Chapter / Section は親モデルの Eager Load の制約クロージャ内(`->with(['chapters' => fn ($q) => $q->ordered()])` のクロージャ内 / `->load(['sections' => fn ($q) => $q->ordered()])` のクロージャ内)。
- **修正範囲**: 上記 3 箇所の一覧取得に並び順昇順の整列(`->ordered()` スコープ)を戻す。各箇所 1 行で完結。トップレベルの整列とリレーション先 Eager Load 内の整列の両方を、漏れなく直す必要がある。

## 補足

### 想定 Q&A

| 質問 | 回答 |
|---|---|
| 並び順の基準は何か? | 各 Part / Chapter / Section に設定された順序番号の昇順。登録日時順ではない |
| 3 階層すべてが対象か? | はい。Part 一覧・Part 詳細画面の Chapter 一覧・Chapter 詳細画面の Section 一覧の 3 つすべてで、並び順(順序番号の昇順)に整列して表示される。症状は同一(整列漏れ)だが、整列を書く場所は階層で異なる |
| 並び順が同値の場合の二次キーは? | 実装依存で可。受け入れ条件は「並び順昇順で表示される」ことであり、同値時の二次整列(登録日時など)は受講生判断 |
