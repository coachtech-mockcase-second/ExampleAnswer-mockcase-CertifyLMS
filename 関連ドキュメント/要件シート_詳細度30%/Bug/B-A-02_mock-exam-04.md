# B-A-02 模試の採点結果が常に不合格になる

## 概要

受講生(student)が模試を提出すると、本来であれば合格点を超える解答(例: 5 問中 4 問正解 = 80%)でも、採点結果が **常に不合格扱い** になる。得点率が想定と異なる小さな値で表示・保存され、合格判定が成立しない。

## 再現手順

**前提**: 受講生でログイン済み、受講中の資格に紐づく公開模試(合格基準点 60% / 問題数 5 問を想定)を 1 つ受験開始済み。

1. 模試を受験中の状態にする(各問に解答)
2. 5 問中 4 問について正解の選択肢を選び、提出する
3. → 採点結果画面に遷移する
4. 採点結果を確認すると、得点率が `0.80`(または `0.80%`)と表示され、判定が **不合格** になっている
5. 全問正解(5 / 5)でも同様に小さな値となり、不合格判定になる

## 期待する結果

得点率は「正解数 ÷ 全問題数」の **百分率(0〜100 のスケール)** で算出され、小数第 2 位まで保持される。5 問中 4 問正解なら `80.00`、5 問中 5 問正解なら `100.00`、3 問正解なら `60.00`。合格基準点(0〜100 の整数)以上で合格、未満で不合格と判定される。

## 実際の結果

得点率が合格基準点と比較できないスケールで保存・表示されてしまい、全問正解でも合格基準点に届かず不合格と判定される。採点結果画面の表示も `0.80` のような違和感のある値になる。

## メタ情報

| 項目 | 値 |
|---|---|
| チケット ID | `B-A-02` |
| Feature 連番 | `mock-exam-04` |
| Feature | mock-exam |
| 種別 | Bug |
| サブカテゴリ | 機能(計算 / 数値スケール誤り) |
| 難易度 | Advance |
| 依存チケット | (なし) |
