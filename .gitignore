# 旧資料（参照用、リポジトリには含めない）
memo/

# Claude Code のローカル設定（個人ごと、共有不要）
.claude/settings.local.json

# Worktree（並列実装用、各セッションで一時的に作成、リポには含めない）
.claude/worktrees/

# 一時的な design 比較スクリーンショット（Playwright で取得、Git には含めない）
/.tmp-design-compare/
/e2e-*.png

# 構築側の作業ファイル（リポジトリには含めない）
docs/steering/content-authoring.md

# macOS のノイズ
.DS_Store
**/.DS_Store
.tmp-design-compare/

# E2E 検証スイート（専用ブランチで管理、main では追跡しない）
/e2e/
